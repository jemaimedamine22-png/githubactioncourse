name: first workflow

on:
  push:
    branches:
      - main 
jobs:
  example-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo "welcome to our first github actios workflow"

      - name: checkout repository
        uses: actions/checkout@v4.2.2

      - name: list repoy files
        run: ls -la
